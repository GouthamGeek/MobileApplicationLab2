<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="goutham.moorthy.s300948360">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".GouthamCheckoutActivity"></activity>
        <activity android:name=".GouthamSemiActivity" />
        <activity android:name=".GouthamDetachedActivity" />
        <activity android:name=".GouthamTownActivity" />
        <activity android:name=".GouthamCondominiumActivity" />
        <activity android:name=".GouthamApartmentActivity" />
        <activity android:name=".GouthamSecondActivity" />
        <activity android:name=".GouthamPaymentActivity" />
        <activity android:name=".GouthamActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>